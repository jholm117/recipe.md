{
  "name": "recipes",
  "version": "1.0.0",
  "description": "- stdin = url to recipe - stdout = markdown",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.15",
    "gh-pages": "^2.1.1",
    "typescript": "^3.7.3"
  }
}
